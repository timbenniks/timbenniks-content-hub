# Database
# For local development: Use your local PostgreSQL or Vercel Postgres connection string
# For Vercel production: Automatically set by Vercel (POSTGRES_URL, POSTGRES_PRISMA_URL, etc.)
DATABASE_URL="postgresql://user:password@host:port/database"

# Auth.js Configuration
# Generate with: openssl rand -base64 32
AUTH_SECRET="your-auth-secret-here"

# App URL
# Local: http://localhost:3000
# Production: https://your-domain.vercel.app
NEXTAUTH_URL="http://localhost:3000"

# Contentstack OAuth
# Region: na, eu, azure-na, azure-eu, gcp-na, gcp-eu
CONTENTSTACK_REGION="eu"

# App UID from Contentstack Developer Hub (not the Client ID!)
CONTENTSTACK_APP_ID="your-app-uid-here"

# OAuth credentials from Contentstack Developer Hub
CONTENTSTACK_CLIENT_ID="your-client-id-here"
CONTENTSTACK_CLIENT_SECRET="your-client-secret-here"

# Cron Secret
# Generate with: openssl rand -base64 32
# Used to secure the daily refresh cron endpoint
CRON_SECRET="your-cron-secret-here"
